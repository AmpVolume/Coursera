##Reflection##
A class is a part of the recipie of the object
For example our object is a cookie.
its yummy, it has choclate, and its crisp on the edges
yet moist on the inside.

Now take a moment and relize what I did right thier
at lines 3 to 5. Did you notice how I described the cookie.
In way didn't I just classify the cookie with these distinctive parts.

##THATS A CLASS## 
It priotiezes defining attributes, properties, behviors, and methods for the object.
In a way this acts as a bluprint because in code classes can usually be refered to many times.

Here is a class example typed in java

public class Car{

//Attrabutes to the car class//
    string: color;
    string: modle;
    int: year;

//Method to display car infmoation//
    void dislpayInfo() {
        system.out.println("Car Modle: " + modle);
        system.out.println("Car Color: " + color);
        system.out.println("Car Year: " + year);
    }
}

First we used public class to make it clear that its a public list that identifies its-self as a Car.
We then use strings, int, float and then a name it such as color or modle like I did in linees 20~22.
Next we need to dispalay the car infromation when its reffered